# Agent framework — we highly recommend the OpenAI Agents SDK (the starter scaffold uses it):
openai-agents>=0.1.0
openai>=1.40
# Prefer Google's Agent Development Kit (ADK)? Use it instead of the two lines above:
#   google-adk>=0.1.0

python-dotenv>=1.0
requests>=2.31

# Lets the Agents SDK (which uses 3.10+ `X | None` syntax) import on Python 3.9.
# Harmless on 3.10+; remove if you're on a clean 3.10+ environment.
eval_type_backport>=0.2

# RAG here is pure-Python keyword/TF-IDF over ~30 short articles (see src/rag.py),
# so no vector store / embeddings dependency is needed.
